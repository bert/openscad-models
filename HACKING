My coding style conventions:

Names of user defined modules in separate files, invoked with [use, include], are in upper-case (like a macro).

Names of toplevel files generated by pcb, are in lower-case.

